// import { CapacitorConfig } from '@capacitor/cli';

const config = {
  appId: 'com.omechu.app',
  appName: '오메추',
  webDir: 'out',
  bundledWebRuntime: false,
  plugins: {
    SplashScreen: {
      launchShowDuration: 0
    }
  }
};

export default config; 